/*create table Note
(
   id integer not null,
   title varchar(255) not null,
   description varchar(255) not null,
   primary key(id)
);

insert into Note (id,title,description)
values('1','PremierTitre', 'Premiere description');
*/